jobs:
  build:
    docker:
      - image: <docker image>
    steps:
      - run: exit 0

      - slack/status:
          mentions: "USERID1,USERID2" 
          fail_only: true 
          webhook: $SLACK_WEBHOOK
          only_for_branches: "master" 

orbs:
  slack: circleci/slack@x.y.z/*
version: 2.1

workflows:
  workflow:
    jobs:
      - build